executor.perCpuMemAllocation = true

profiles {
    slurm {
        process.executor = 'slurm'
        process.cpus = 4
        process.memory = '16GB'
        process.scratch=true
        stageInMode = 'symlink'
        stageOutMode = 'rsync'
        queueSize = 10
    }
}